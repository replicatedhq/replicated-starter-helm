name: Create release on Unstable channel

on:
  push:
   branches:
     - master

jobs:
  create-unstable-release:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Create a release
      run: make release

